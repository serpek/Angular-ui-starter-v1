import template from './committee-actions.html';
import controller from './committee-actions.controller';
import './committee-actions.scss';

let committeeActionsComponent = {
  bindings: {},
  template,
  controller
};

export default committeeActionsComponent;
